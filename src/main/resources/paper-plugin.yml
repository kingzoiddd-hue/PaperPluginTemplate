name: Example
version: '1.0.0'
main: dev.example.example.Example
api-version: '1.21.11'
folia-supported: true
dependencies:
  server:
    packetevents:
      load: BEFORE
      required: true